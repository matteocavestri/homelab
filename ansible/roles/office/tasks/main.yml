---
- name: Ensure destination directories exist (office)
  ansible.builtin.file:
    path: "{{ (item.dest | dirname) }}"
    state: directory
    mode: "0755"
  loop: "{{ office_files }}"
  loop_control:
    label: "{{ item.dest }}"

- name: Deploy templated quadlet/config files (office)
  ansible.builtin.template:
    src: "{{ item.src }}"
    dest: "{{ item.dest }}"
    mode: "{{ item.mode }}"
  loop: "{{ office_files }}"
  loop_control:
    label: "{{ item.dest }}"
  notify: "{{ item.notify + ['daemon-reload'] }}"
