---
- name: Ensure destination directories exist (proxy)
  ansible.builtin.file:
    path: "{{ (item.dest | dirname) }}"
    state: directory
    mode: "0755"
  loop: "{{ proxy_files }}"
  loop_control:
    label: "{{ item.dest }}"

- name: Deploy templated quadlet/config files (proxy)
  ansible.builtin.template:
    src: "{{ item.src }}"
    dest: "{{ item.dest }}"
    mode: "{{ item.mode }}"
  loop: "{{ proxy_files }}"
  loop_control:
    label: "{{ item.dest }}"
  notify: "{{ item.notify + ['daemon-reload'] }}"
