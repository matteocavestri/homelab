---
- name: Ensure destination directories exist (common)
  ansible.builtin.file:
    path: "{{ (item.dest | dirname) }}"
    state: directory
    mode: "0755"
  loop: "{{ common_files }}"
  loop_control:
    label: "{{ item.dest }}"

- name: Deploy templated quadlet/config files (common)
  ansible.builtin.template:
    src: "{{ item.src }}"
    dest: "{{ item.dest }}"
    mode: "{{ item.mode }}"
  loop: "{{ common_files }}"
  loop_control:
    label: "{{ item.dest }}"
  notify: "{{ item.notify + ['daemon-reload'] }}"
